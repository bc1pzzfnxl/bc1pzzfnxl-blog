---
// src/layouts/MainLayout.astro
import { Icon } from "astro-icon/components";
import "@fontsource/geist-sans/400.css";
import "@fontsource/geist-sans/500.css";
import "@fontsource/geist-sans/700.css";
import "@fontsource/geist-mono/400.css";

const { title } = Astro.props;
---

<html lang="en">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <title>{title}</title>
        <link id="favicon-link" rel="icon" type="image/svg+xml" href="/favicon-dark.svg" />
        <script is:inline>
            // This script is placed in the head to prevent FOUC (Flash of Unstyled Content)
            // It runs before the page is rendered to apply the correct theme.
            (function() {
                const theme = localStorage.getItem('theme');
                const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
                const isDark = theme === 'dark' || (!theme && prefersDark);
                if (isDark) {
                    document.documentElement.classList.add('dark-mode');
                }
                
                // Update favicon based on theme
                window.addEventListener('DOMContentLoaded', () => {
                    const favicon = document.getElementById('favicon-link');
                    if (favicon) {
                        favicon.href = isDark ? '/favicon-light.svg' : '/favicon-dark.svg';
                    }
                });
            })();
        </script>
        <style is:global>
            :root {
                --body-font: 'Geist Sans', -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif;
                --mono-font: 'Geist Mono', monospace;
            }

            body {
                max-width: 680px;
                margin: 0 auto;
                padding: 60px 24px;
                font-family: var(--body-font);
                font-size: 18px;
                line-height: 1.65;
                color: #374151; /* Dark gray for less harsh reading */
                background-color: #ffffff;
                transition: background-color 0.3s ease, color 0.3s ease;
                -webkit-font-smoothing: antialiased;
                -moz-osx-font-smoothing: grayscale;
            }

            /* Minimalist Header */
            header {
                margin-bottom: 80px;
                display: flex;
                justify-content: space-between;
                align-items: center;
            }
            .header-left {
                display: flex;
                align-items: center;
                gap: 12px;
            }
            .site-logo {
                width: 32px;
                height: 32px;
            }
            .bear-black { display: block; }
            .bear-white { display: none; }
            html.dark-mode .bear-black { display: none; }
            html.dark-mode .bear-white { display: block; }

            h1, h2, h3, h4, .site-title a {
                color: #111827; /* Pure dark for titles */
                font-weight: 600;
                letter-spacing: -0.02em;
                line-height: 1.25;
            }

            h1.site-title a {
                text-decoration: none;
                font-size: 1.25rem;
            }

            .theme-toggle-button {
                background: none;
                border: none;
                cursor: pointer;
                padding: 5px;
                border-radius: 50%;
                display: flex;
                align-items: center;
                justify-content: center;
                opacity: 0.7;
                transition: opacity 0.2s ease;
            }
            .theme-toggle-button:hover {
                opacity: 1;
            }
            .theme-toggle-button .icon {
                width: 22px;
                height: 22px;
                color: #111;
            }

            /* Links and list */
            a {
                color: #111;
                text-decoration: underline;
                text-decoration-thickness: 1.5px;
                text-underline-offset: 4px;
                transition: color 0.2s ease, background-color 0.2s ease;
            }
            a:hover {
                background-color: #111;
                color: #fff;
                text-decoration: none;
            }

            ul {
                list-style: none;
                padding: 0;
            }
            li {
                margin-bottom: 16px;
                display: flex;
                align-items: baseline;
            }

            /* Article Typography */
            article h1 {
                font-size: 2.25rem;
                margin-top: 2rem;
                margin-bottom: 1rem;
            }
            article h2 {
                font-size: 1.75rem;
                margin-top: 2.5rem;
                margin-bottom: 0.75rem;
            }
            article p {
                margin-bottom: 1.75rem;
            }
            article ul {
                list-style: disc;
                padding-left: 24px;
                margin-bottom: 2rem;
            }
            article li {
                display: list-item;
                margin-bottom: 0.75rem;
            }
            .back-link {
                display: inline-block;
                margin-top: 60px;
                font-size: 0.95rem;
                opacity: 0.6;
            }
            
            /* Dark Mode Styles */
            html.dark-mode body {
                background-color: #111111; 
                color: #d1d5db; /* Softer gray for dark mode body text */
            }
            html.dark-mode h1, 
            html.dark-mode h2, 
            html.dark-mode h3, 
            html.dark-mode h4,
            html.dark-mode h1.site-title a,
            html.dark-mode a {
                color: #ffffff; /* Bright white for titles/links */
            }
            html.dark-mode a:hover {
                background-color: #ffffff;
                color: #111;
            }
            html.dark-mode .theme-toggle-button .icon {
                color: #fff;
            }
            .moon-icon { display: none; }
            .sun-icon { display: block; }
            html.dark-mode .moon-icon { display: block; }
            html.dark-mode .sun-icon { display: none; }
        </style>
    </head>
            .theme-toggle-button {
                background: none;
                border: none;
                cursor: pointer;
                padding: 5px;
                border-radius: 50%;
                display: flex;
                align-items: center;
                justify-content: center;
            }
            .theme-toggle-button .icon {
                width: 24px;
                height: 24px;
                color: #111;
            }

            /* Links and list */
            a {
                color: #111;
                text-decoration: underline;
                text-decoration-thickness: 1px;
                text-underline-offset: 3px;
            }
            a:hover {
                background-color: #000;
                color: #fff;
                text-decoration: none;
            }

            ul {
                list-style: none;
                padding: 0;
            }
            li {
                margin-bottom: 12px;
                display: flex;
                align-items: baseline;
            }

            /* Article Typography */
            article h1 {
                font-size: 1.8rem;
                margin-bottom: 0.5rem;
                letter-spacing: -1px;
            }
            article p {
                margin-bottom: 1.5rem;
            }
            article ul {
                list-style: disc;
                padding-left: 20px;
                margin-bottom: 1.5rem;
            }
            article li {
                display: list-item;
                margin-bottom: 0.5rem;
            }
            .back-link {
                display: inline-block;
                margin-top: 40px;
                font-size: 0.9rem;
                opacity: 0.6;
            }
            
            /* Dark Mode Styles */
            html.dark-mode body {
                background-color: #111; /* Black background */
                color: #eee; /* Off-white text (less harsh) */
            }
            html.dark-mode h1.site-title a,
            html.dark-mode a {
                color: #fff; /* White links */
            }
            html.dark-mode a:hover {
                color: #d946ef; /* Keep your accent color */
            }
            html.dark-mode .theme-toggle-button .icon {
                color: #fff;
            }
            .moon-icon { display: none; }
            .sun-icon { display: block; }
            html.dark-mode .moon-icon { display: block; }
            html.dark-mode .sun-icon { display: none; }
        </style>
    </head>
    <body>
        <header>
            <div class="header-left">
                <img src="/favicon-dark.svg" alt="Bear icon" class="site-logo bear-black" />
                <img src="/favicon-light.svg" alt="Bear icon" class="site-logo bear-white" />
                <div class="site-title"><a href="/">bc1pzzfnxl</a></div>
            </div>
            <button class="theme-toggle-button" id="theme-toggle">
                <Icon name="heroicons-solid:sun" class="sun-icon icon" />
                <Icon name="heroicons-solid:moon" class="moon-icon icon" />
            </button>
        </header>
        <main>
            <slot />
        </main>
        <script>
            const themeToggle = document.getElementById('theme-toggle');
            
            themeToggle.addEventListener('click', () => {
                const isDarkMode = document.documentElement.classList.toggle('dark-mode');
                localStorage.setItem('theme', isDarkMode ? 'dark' : 'light');
                
                // Update favicon when toggled
                const favicon = document.getElementById('favicon-link');
                if (favicon) {
                    favicon.href = isDarkMode ? '/favicon-light.svg' : '/favicon-dark.svg';
                }
            });
        </script>
    </body>
</html>